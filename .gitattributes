# Force LF for the Bradbury build artifact + its minifier: CRLF would pad the
# deploy source (36,902 B -> 37,676 B), shrinking the margin against Bradbury's
# BlockPubdataLimitReached pubdata cap for no benefit.
intelligent-contracts/proofmark-bradbury.py            text eol=lf
e2e/minify_contract.py text eol=lf
